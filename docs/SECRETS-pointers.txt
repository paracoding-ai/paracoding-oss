# SECRETS (host-side only, NEVER in this repo):
- /etc/fleet/gh_token (GitHub org token)
- /root/ops/secret.keys (third-party API keys)
- /opt/paracoding-mcp/connector-urls.txt (per-agent MCP secret paths)
- paracoding-infra gcloud user creds + ADC (~/.config/gcloud)
For DR: mint fresh, install root-only / GCP Secret Manager. Nginx secret paths are regenerated per agent.
