{
  "note": "Routes that are PUBLIC (no auth guard found in the handler) as of the commit that created this file. A NEW public route not listed here fails the build. Removing an entry here is how you assert a route has been given a guard. Adding one is a deliberate decision to ship a public route -- say why in the commit message. | 2026-08-01: assertIdentity added to BOTH scanners (route-audit.mjs and gen-baseline.py, kept in step deliberately). 5 route(s) that were always bearer-token guarded are no longer listed public. Verified a strict subset of the previous baseline -- nothing newly blessed. | 2026-08-09 [SEC-ROUTE-RETIRE-V2]: ONE entry removed -- the retired legacy console page alias whose whole handler was a 301 to /harness. That route is deleted from src/index.ts in this SAME commit under a standing order that its name must not appear in the source, which is also why neither this note nor the source names it. This is the deliberate deletion of a VANISHED route, not the blessing of a new guard: it was only ever listed public because a one-line redirect handler contains no guard token, while the harness app.use middleware actually gated it -- and that middleware entry is deleted too. Exactly one line was removed. The baseline was NOT regenerated. | 2026-08-09 [SEC-COWORK-PROMPT-RETIRE-V1]: GET /api/cowork-prompt DELETED from src/index.ts per the operator's 2026-08-04 ruling. The `public` list is UNCHANGED and no entry was removed from it: that route carried a waGate and was therefore never listed public, so route-audit.mjs's vanished-route check does not fire. What did change is the surface_split counts below -- 87 -> 86 total, 71 -> 70 guarded, console 62 -> 61 -- because smoke.py F7.1/F7.2 compare against them. Counted directly from PC_SURFACE_MAP after the edit: 86 entries, 61 console + 25 mcp, zero mapped-but-unregistered and zero registered-but-unmapped. The MCP tool `cowork_prompt` and harCoworkPromptTool are a SEPARATE thing and deliberately survive; only the HTTP route is gone. | 2026-08-10 [SEC-LEGACY-SSE-MCP-RETIRE-V1]: the legacy 2024-11-05 HTTP+SSE MCP transport -- GET /api/mcp, POST /api/mcp and the activeConnections in-memory Map -- is DELETED from src/index.ts in this SAME commit, and EXACTLY ONE line was removed from the `public` list above: \"POST /api/mcp\". This is the deliberate deletion of a VANISHED route, not the blessing of a new guard. WHY, security first: POST /api/mcp called no guard at all -- its only credential was possession of the connectionId minted by the GET, a session id used as a bearer token, on the IAP-OFF mcp surface -- and it wrote pending_confirms with staged_by taken from that connection, so anyone holding a leaked connection id could forge attribution on the human approval board, the one surface where attribution must be correct. It was also already non-functional: with scale-to-zero and no session affinity the GET and its POST can land on different Cloud Run instances (400), and it never implemented `initialize`, so no MCP client could complete a handshake. The modern stateless POST /mcp is untouched and unaffected. GET /api/mcp was never listed public (it called assertIdentity) so its removal costs no line here; what changed with it are the counts below -- 86 -> 84 total, 70 -> 69 guarded, 16 -> 15 public, mcp 25 -> 23 -- because smoke.py F7.1/F7.2 compare against them. Counted directly from PC_SURFACE_MAP after the edit: 84 entries, 61 console + 23 mcp, zero mapped-but-unregistered and zero registered-but-unmapped, and `node route-audit.mjs src/index.ts route-baseline.json` prints 84/69/15 PASS. The baseline was NOT regenerated. POST /mcp/:token is a separate, stateless transport and deliberately survives. | 2026-08-10 [MCP2026-DUAL-ERA-V1]: EXACTLY ONE route added -- DELETE /mcp -- and the `public` list above is UNCHANGED, because that handler calls oaBearerRole and is therefore GUARDED. It mirrors GET /mcp line for line: unauthenticated gets the OAuth challenge, authenticated gets 405 method_not_allowed. WHY IT EXISTS: the 2026-07-28 transport mints no session, so DELETE-to-terminate is gone, and leaving the verb unregistered means Express answers a bare 404 -- which a dual-era client reads as `no modern MCP endpoint here` and follows by probing the deprecated HTTP+SSE GET transport, a failure whose diagnostic points at the wrong thing. The counts in surface_split move with it because smoke.py F7.1/F7.2 compare against them: 84 -> 85 total, 69 -> 70 guarded, mcp 23 -> 24; public stays 15 and console stays 61. Counted from the edited bytes, not inherited: PC_SURFACE_MAP has 85 entries (61 console + 24 mcp) and `node route-audit.mjs src/index.ts route-baseline.json` prints 85/70/15 ROUTE AUDIT PASS. The baseline was NOT regenerated -- five numbers changed and this note was added. POST /mcp is unchanged in the audit's eyes: the new era router and the modern branch it calls are module-scope functions ABOVE the registration, so the handler body the audit scans still contains no guard token and POST /mcp stays listed public exactly as before. | 2026-08-10 [SEC-ROUTE-BLANKER-REGEX-V2]: EXACTLY ONE entry ADDED -- \"POST /api/jobs/fire\" -- and it is added because a BUG IN THE AUDIT was fixed, NOT because a guard was removed. route-audit.mjs's blankComments() tracked ' \" and ` and nothing else, so the two ['\"] character classes in the waIsDangerous regex literal at src/index.ts:2209 desynchronised its quote state and 406 comment lines (2216-3691) were never blanked. POST /api/jobs/fire is registered at 2948 and the handler body the audit scans runs to 3034; at line 3031 a COMMENT describing the NEXT route (/api/jobs/supersede) contains the word oaBearerRole, and that PROSE ALONE marked this handler guarded -- the exact failure [SEC-ROUTE-VISIBILITY-V1] closed, reopened by a different mechanism. With the blanker fixed the prose is blanked and the handler is correctly seen to contain no guard token. THE ROUTE IS NOT UNAUTHENTICATED AND NOTHING ABOUT ITS SECURITY CHANGED IN THIS COMMIT: it is authenticated by a single-use run_token compared with the constant-time waEq() against the stored value, and additionally requires status 'preapproved', a 12h expiry and a cmd_sha match, with any failure reverting status and returning 409 having executed nothing. src/index.ts:2947 already tags it AUTH-NON-SESSION for exactly this reason. It is listed here because this file's `public` means literally 'no auth guard token found in the handler', which is now true of it. REJECTED ALTERNATIVE: adding waEq to GUARDS would have held the counts at 70/15 and today flips only this one route, but waEq is a generic constant-time string compare, not a named guard, so blessing it would mark ANY future handler that compares two strings as guarded -- a far broader hole than the single line it saves. MEASURED over the whole file: the blanker fix changes 5793 contiguous byte runs, of which 5789 are comments the old scanner failed to blank (407 source lines, 2216-3691) and 4 are REAL CODE the old scanner DESTROYED at line 5517, where the escaped slash in .replace(/\\//g, '_') was read as a line comment and the rest of the line erased. Exactly ONE route verdict moves across all 85. The counts below move because smoke.py F7.1/F7.2 compare against them: guarded 70 -> 69, public 15 -> 16; total stays 85 and console 61 / mcp 24 are unchanged because NO route was added or removed. The baseline was NOT regenerated -- one line added, four numbers changed, this note appended. | 2026-08-12 [SEC-ROUTE-VANISH-ANY-V1]: THE `registered` LIST BELOW IS NEW AND IT IS THE WHOLE OF THIS CHANGE. Until this commit route-audit.mjs could only notice the loss of a route THIS FILE LISTED AS PUBLIC -- 16 of 85 -- because the vanished-route check ran over `public` and nothing else. Delete the whole registration of a GUARDED route and the build PASSED. MEASURED at main 0b554a54 against the UNEDITED audit and the UNEDITED baseline, not argued: deleting the registration of GET /api/models printed 84/68/16 and ROUTE AUDIT PASS, exit 0; deleting GET /dash from the same tree printed 84/69/15 and exit 1 naming it. Sixty-nine of eighty-five routes could be lost in a merge without a word, and the wiki recorded that as a known limit instead of a defect. WHAT `registered` IS: the flat, sorted set of METHOD PATH keys for EVERY route the audit can see, guarded and public alike. NOT A COUNT -- a count cannot name the route that went, which is the only output worth printing, and it is satisfied by any swap that loses one route and adds another; the [SEC-ROUTE-F71-REPORT-V1] block in route-audit.mjs already settled that two scanners can agree on 85/69/16 and disagree about WHICH route is which, so the SET is the contract and the count is a convenience. NOT THE GUARDED VERDICT -- guard LOSS is already a hard failure through `public` (a handler that loses its guard token becomes a NEW public route), guard GAIN is a deliberate non-failure printed as `now guarded`, and recording the verdict a second time would fail the build on every legitimate guard addition while creating two sources of truth for one fact. NOT THE SURFACE -- the surface_split ruling below stands unchanged: this audit parses SOURCE as a build step before esbuild, knows nothing of which Cloud Run service registers what, and must not be taught here; per-surface facts stay in the key the audit provably never reads. NO COUNT IS HARD-CODED IN THE AUDIT BY THIS CHANGE: `registered` is data and its length is read off it at run time, so the numbers in surface_split remain the only counts in this file and they move exactly as they always have. HOW A DELIBERATE REMOVAL IS RECORDED, which is the only way to make this gate stand down: delete exactly that route's line from `registered`, and its line from `public` too if it has one, in the SAME commit as the source change, and say why in the commit message. A DELIBERATE ADDITION costs one line in `registered` in the same commit -- the audit refuses a registered route this file does not name, because a list that is allowed to go stale stops being a ratchet: a route added today and left unrecorded could be dropped tomorrow with nothing to compare against, and the same check catches a merge that RESURRECTS a route the fleet deliberately deleted. --write-baseline NO LONGER EXITS 0: it writes the measured lists and then FAILS the run, the same property PC_LEAK_BASELINE_WRITE has in oss/gen.py, so no build can re-measure its own gate and report success in the same breath. It also emits only note, generated_from, public and registered, so it DESTROYS this change log and the whole surface_split block -- another reason it is not the way a removal is recorded. control-plane/gen-baseline.py, the python half kept in step with the audit for the gate executor that has no node, does NOT emit `registered` and is unchanged here; a baseline regenerated with it now fails the audit LOUDLY (exit 2, `has no registered list`) rather than quietly switching the gate off. THE PARALLEL RETIREMENT OF POST /api/confirm/verify TOUCHES BOTH LISTS AND THAT IS THE ONLY INTERACTION: it is listed public, so it takes TWO deletions here -- the identical line \"POST /api/confirm/verify\" out of `public` and out of `registered` -- alongside the surface_split moves that change already owed: audit_visible_total and total 85 -> 84, audit_visible_public and public_count 16 -> 15, mcp 24 -> 23, with audit_visible_guarded and guarded staying 69 and console staying 61 because the route is public and on the mcp surface. No other line in this file moves with it. | 2026-08-12 [SEC-LEGACY-CONFIRM-RETIRE-V1]: the legacy shared-secret confirm route -- POST /api/confirm/verify -- is DELETED from src/index.ts in this SAME commit, and EXACTLY ONE line was removed from EACH of the `public` and `registered` lists above. Both are required now that route-audit.mjs refuses a vanished route whether it was guarded or public, and refuses a live route the baseline does not record; leaving either line in place fails the build, which is the intended behaviour and was rehearsed against exactly this removal. This is the deliberate deletion of a VANISHED route, not the blessing of a new guard. WHY, security first: it approved privileged jobs on a single SHARED BEARER SECRET presented in an x-human-token header -- no passkey, no binding to what the operator was shown, no danger classification, no approver allowlist, and none of the approval stamping (approved_sha256 / approval_mac / approval_sig) every other approval path performs -- so possession of one long-lived string was a full approval capability on the IAP-OFF mcp surface. It was ALSO already incapable of executing anything: on 'confirmed' it POSTed ${GATE_EXEC_URL}/run with NO Authorization header, which the edge drops because gate-exec is private, so the job was left stranded in 'confirmed' having run nothing -- the operator's approval was spent and the work did not happen. Retired WITH ITS KEY: humanTokenOk(), HUMAN_CONFIRM_SECRET, its 16-char floor and the boot warning are deleted from src/index.ts, the HUMAN_CONFIRM_SECRET binding is removed from BOTH --set-secrets lines in the installer, and the installer no longer CREATES pc-<lp>human-confirm-secret or grants the control-plane SA access to it. uninstall.sh still DELETES that secret and the installer still classifies it as ours in its inventory, both deliberately: an existing install holds one and must still be able to clean it up. POST /api/confirm/stage is a DIFFERENT route on a DIFFERENT credential (assertIdentity) and deliberately survives -- it stages, it does not approve. POST /api/webauthn/confirm/verify, the passkey path the gate actually calls, is untouched. The MCP tool `list_pending_confirm` deliberately survives: it is a pure Firestore reader over the pending_confirms collection and never called this route. The counts below move because smoke.py F7.1/F7.2 compare against them: total 85 -> 84, mcp 24 -> 23, public 16 -> 15; guarded stays 69 because the removed route was PUBLIC in this file's sense (its handler contained no guard token), and console stays 61 because the route was mcp. Counted directly from PC_SURFACE_MAP after the edit: 84 entries, 61 console + 23 mcp, zero mapped-but-unregistered and zero registered-but-unmapped, and `node route-audit.mjs src/index.ts route-baseline.json` prints 84/69/15 ROUTE AUDIT PASS. The baseline was NOT regenerated -- two lines removed, five numbers changed, this note appended. | 2026-08-15 [PCGIT-UPLOAD-V1]: EXACTLY ONE route ADDED -- POST /git/blob -- and the `public` list above is UNCHANGED, because that handler calls pcUploadCaller and is therefore GUARDED. WHY IT EXISTS: GET /git/archive moves the tree OUT over HTTP so no model has to read it; there was no counterpart to get bytes IN, so landing a large file meant a model retyping it into git_propose's `content`, which is the single biggest cost in this project and has corrupted files before. POST /git/blob takes raw bytes into the git object store and records which agent supplied them; git_propose's new `uploaded` option then resolves ONLY against that agent's own unexpired record, so an oid is still never a lookup key. THE GUARD IS THE AGENT SESSION KEY, resolved by the SAME pcSessionLookup every tool call goes through -- no new credential type -- and 'pcUploadCaller' was added to GUARDS in route-audit.mjs for the same reason 'pcArchiveCaller' was: a real guard the scanner cannot see would make the route read as newly unauthenticated and fail the build for the wrong reason. The counts below move because smoke.py F7.1/F7.2 compare against them: total 82 -> 83, guarded 67 -> 68, mcp 24 -> 25; public stays 15 and console stays 58. Counted directly from PC_SURFACE_MAP after the edit: 83 entries, 58 console + 25 mcp. `node route-audit.mjs src/index.ts route-baseline.json` prints 83/68/15 ROUTE AUDIT PASS. The baseline was NOT regenerated -- one line added to `registered`, five numbers changed, this note appended.",
  "generated_from": "src/index.ts",
  "public": [
    "GET /.well-known/agent.json",
    "GET /.well-known/agents",
    "GET /.well-known/oauth-authorization-server",
    "GET /.well-known/oauth-authorization-server/mcp",
    "GET /.well-known/oauth-protected-resource",
    "GET /.well-known/oauth-protected-resource/mcp",
    "GET /.well-known/openid-configuration",
    "GET /agents/:role/.well-known/agent-card.json",
    "GET /agents/:role/.well-known/agent.json",
    "GET /dash",
    "GET /favicon.ico",
    "GET /icon.png",
    "POST /api/jobs/fire",
    "POST /api/webauthn/unlock/options",
    "POST /api/webauthn/unlock/verify",
    "POST /mcp",
    "POST /oauth/register"
  ],
  "registered": [
    "DELETE /mcp",
    "GET /",
    "GET /.well-known/agent.json",
    "GET /.well-known/agents",
    "GET /.well-known/oauth-authorization-server",
    "GET /.well-known/oauth-authorization-server/mcp",
    "GET /.well-known/oauth-protected-resource",
    "GET /.well-known/oauth-protected-resource/mcp",
    "GET /.well-known/openid-configuration",
    "GET /agents/:role/.well-known/agent-card.json",
    "GET /agents/:role/.well-known/agent.json",
    "GET /api/chat/history",
    "GET /api/dash/gcp",
    "GET /api/dash/summary",
    "GET /api/dash/usage",
    "GET /api/fleet/agents",
    "GET /api/flow",
    "GET /api/github/status",
    "GET /api/keys/status",
    "GET /api/models",
    "GET /api/oauth/allowed",
    "GET /api/ops/session",
    "GET /api/security/pqc-tls",
    "GET /api/sessions",
    "GET /api/sessions/roles",
    "GET /api/strains",
    "GET /api/usage",
    "GET /api/webauthn/job/:id",
    "GET /api/webauthn/pending",
    "GET /api/webauthn/status",
    "GET /brand/logo.png",
    "GET /chat",
    "GET /dash",
    "GET /favicon.ico",
    "GET /flow",
    "GET /flowhood",
    "GET /git/archive",
    "GET /harness",
    "GET /icon.png",
    "GET /lakeview",
    "GET /mcp",
    "GET /oauth/authorize",
    "GET /oauth/strains",
    "GET /wiki",
    "GET /wiki/:slug",
    "GET /wiki/assets/:name",
    "POST /api/chat",
    "POST /api/confirm/stage",
    "POST /api/github/config",
    "POST /api/github/token",
    "POST /api/jobs/fire",
    "POST /api/jobs/supersede",
    "POST /api/journal/log",
    "POST /api/keys",
    "POST /api/lakeview/link",
    "POST /api/oauth/allowed",
    "POST /api/ops/end",
    "POST /api/ops/token",
    "POST /api/queue/claim",
    "POST /api/queue/post",
    "POST /api/security/pqc-tls",
    "POST /api/sessions/mint",
    "POST /api/sessions/revoke",
    "POST /api/sessions/roleflags",
    "POST /api/strain/create",
    "POST /api/strain/delete",
    "POST /api/strain/subculture",
    "POST /api/strains/provision",
    "POST /api/strains/retire",
    "POST /api/webauthn/confirm/options",
    "POST /api/webauthn/confirm/verify",
    "POST /api/webauthn/elevate/options",
    "POST /api/webauthn/elevate/verify",
    "POST /api/webauthn/enroll/link",
    "POST /api/webauthn/enroll/options",
    "POST /api/webauthn/enroll/verify",
    "POST /api/webauthn/preapprove",
    "POST /api/webauthn/register/options",
    "POST /api/webauthn/register/verify",
    "POST /api/webauthn/unlock/options",
    "POST /api/webauthn/unlock/verify",
    "POST /git/blob",
    "POST /mcp",
    "POST /mcp/:token",
    "POST /oauth/authorize/complete",
    "POST /oauth/authorize/key",
    "POST /oauth/register",
    "POST /oauth/token"
  ],
  "surface_split": {
    "note": "2026-08-09 [SEC-SURFACE-SMOKE-V1]. The release now serves this ONE route table from TWO Cloud Run services built from ONE image (console: PC_SURFACE=console, IAP on; mcp: PC_SURFACE=mcp, IAP off). DECISION: the `public` list above stays WHOLE-IMAGE and its meaning is unchanged. route-audit.mjs parses src/index.ts SOURCE, runs as a build step BEFORE esbuild, and hard-fails the image; it knows nothing of runtime surfaces and must not be taught about them here. Per-surface facts are recorded in THIS key instead, which route-audit.mjs provably ignores -- it reads only baseline.public. Splitting `public` per surface would also be wrong on the merits: a route's guard is a property of its handler, not of which service happens to register it, and the public set is identical either way.  | 2026-08-16 [WIKI-ASSETS-V77]: EXACTLY ONE route ADDED -- GET /wiki/assets/:name -- and the `public` list is UNCHANGED, because the handler calls waSessionOk and is therefore GUARDED. It serves the five architecture diagrams out of the container image; they are baked beside the HTML rather than read from the lake because harReadLake returns a STRING and a PNG through that path is corrupted, and because _shell.html blocks data: URIs outright. The name is matched against a five-entry Set before anything touches disk, so traversal is unreachable rather than filtered. Counts moved by exactly one each: total, audit_visible_total, guarded, audit_visible_guarded and console; public_count, mcp and both are unchanged. The baseline was NOT regenerated -- one line added to `registered`, five numbers incremented, this note appended.  | 2026-08-16 [WIKI-ASSETS-V77] SECOND NOTE, ON NUMBERS THAT WERE ALREADY WRONG BEFORE THIS COMMIT: incrementing the previous values by one would have propagated a stale count, so they were RE-MEASURED instead. The file carried total 83 / guarded 68 / console 58; route-audit.mjs over the PRE-change tree printed 86/71/15 and PC_SURFACE_MAP held 86 entries. Four routes had been added since the counts were last touched without this block being updated -- route-audit.mjs provably never reads surface_split, so nothing failed and the drift was invisible. smoke.py F7.1/F7.2 DO compare against these numbers, so they were wrong where it mattered. They are now counted directly from PC_SURFACE_MAP and from the audit's own output over the POST-change tree: 87 entries, 72 guarded, 15 public.  | 2026-08-16 [BRAND-SELFHOST-V80]: THREE routes ADDED -- GET /brand/logo.png (console, guarded by waSessionOk) and GET /favicon.ico + GET /icon.png (both surfaces, and DELIBERATELY PUBLIC, which is why two lines are added to the `public` list above). A favicon is requested by a browser before any session can exist, and an MCP client fetches logo_uri with no credential; guarding either would mean shipping a route that can only ever 401. They serve static image bytes from the container and read nothing. This replaces three <img> tags in harness.html that fetched the logo from a third-party domain. Counts re-counted from PC_SURFACE_MAP after the edit.  | 2026-08-24 [SEC-SURFACE-COUNT-RESYNC-V1]: NO ROUTE IS ADDED OR REMOVED BY THIS EDIT. Only these counters move, because they were THREE ROUTES STALE and smoke.py F7.1/F7.2 compare against them -- F7.1.ROUTE_TABLE_UNCHANGED and F7.2.SURFACE_PARTITION_TOTAL would both have FAILED on an unmodified tree, reporting the route table as MOVED for reasons unrelated to whoever ran them. MEASURED, not inherited: `node route-audit.mjs control-plane/src/index.ts control-plane/route-baseline.json` prints total 93 / guarded 76 / public 17 and ROUTE AUDIT PASS, and PC_SURFACE_MAP parses to 93 entries split console 65 / mcp 23 / both 5. This block carried total 90 / guarded 73 / console 62. THE THREE ROUTES THAT LANDED WITHOUT THIS BLOCK BEING TOUCHED are the [GH-TOOLS-V1] operator panel: GET /api/github/status, POST /api/github/token and POST /api/github/config. All three are console-surface and all three call waGate, which is exactly the observed +3 total / +3 guarded / +3 console with public_count, mcp and both unchanged; no other combination of the live table produces that shape. HOW THEY WERE IDENTIFIED, since no note recorded them: their PC_SURFACE_MAP entries are the only console block added after the [BRAND-SELFHOST-V80] measurement above, and the comment sitting on those entries records that omitting them took the FIRST v10.2 MCP REVISION DOWN AT BOOT -- a v10.2 landing, i.e. after the 2026-08-16 count this block last recorded. WHY THE DRIFT WAS INVISIBLE: `registered` already listed all 93 (the audit refuses a live route it does not name, so whoever added them updated that list), and route-audit.mjs provably never reads surface_split. HAND-EDITED ON PURPOSE: `route-audit.mjs --write-baseline` emits only note, generated_from, public and registered, so it would DESTROY this change log and the whole surface_split block. Five numbers changed (audit_visible_total, audit_visible_guarded, total, guarded, console), this note appended, and nothing else in this file.",
    "surface_map_symbol": "PC_SURFACE_MAP in control-plane/src/index.ts",
    "audit_visible_total": 88,
    "audit_visible_guarded": 71,
    "audit_visible_public": 17,
    "total": 88,
    "guarded": 71,
    "public_count": 17,
    "console": 60,
    "mcp": 23,
    "both": 5,
    "audit_blind_spots": [],
    "blind_spot_note": "RE-MEASURED 2026-08-09 [SEC-ROUTE-RETIRE-V2] at main 045dd7e7 -- THERE IS NO BLIND SPOT LEFT, so audit_blind_spots is now empty and audit_visible_* equals the real totals. Evidence: `node route-audit.mjs src/index.ts route-baseline.json` over the PRE-change index.ts (blob e8f6f3b8) printed 88/71/17 and PASS; over the POST-change index.ts it prints 87/71/16. The hole closed because [SEC-ROUTE-VISIBILITY-V1] de-indented GET /api/cowork-prompt and made any registration off column zero a hard build failure. PC_SURFACE_MAP was counted directly, not inferred: 88 entries (63 console / 25 mcp) before, 87 (62 console / 25 mcp) after. The original note is kept below because it is the only record of why the two totals ever differed. MEASURED 2026-08-09 at main 03697fdd, index.ts blob db767766. route-audit.mjs's regex is anchored at column zero (/^app\\./gm), so a registration indented into an if or a try is invisible to it. GET /api/cowork-prompt is registered at index.ts:3651 indented inside a try{} and is therefore NOT scanned: its handler is never searched for a guard, so removing that guard would never be reported as a new public route. That is a hole in the guard scanner, not merely a count. It is why audit_visible_total (87) and total (88) differ, and both are recorded rather than reconciled.",
    "disappearance_defect": "CLOSED -- DO NOT ACT ON THE SUPERSEDED TEXT BELOW. Re-measured 2026-08-09 [SEC-ROUTE-RETIRE-V2] against route-audit.mjs blob d54e3634: deleting one route from src/index.ts and running the audit against the UNEDITED baseline exits 1 with 'ROUTE AUDIT FAIL: 1 baseline route(s) NO LONGER REGISTERED'. A vanished route is a HARD FAILURE in route-audit.mjs itself now, which is exactly why the one-line deletion above had to land in the SAME commit as the source change. smoke.py F7.1/F7.2/F7.3 stay as defence in depth. SUPERSEDED ORIGINAL: MEASURED 2026-08-09: route-audit.mjs EXITS 0 WHEN A ROUTE VANISHES. It hard-fails only on a NEW public route, a wildcard, or a missing connector route. Indenting one guarded registration moved it to 86/69/17 and still PASSED; indenting a PUBLIC one (GET /dash) moved it to 86/70/16 and PASSED while printing 'now guarded (was public): GET /dash' -- reporting a disappeared route as a security IMPROVEMENT. This cannot be fixed from the baseline, which is inert data that route-audit.mjs only reads `public` out of, and fixing it in route-audit.mjs risks bricking the build for a file this lane does not own. It is therefore ENFORCED in shared/state/security-lane/devgate/smoke.py -- F7.1.ROUTE_TABLE_UNCHANGED catches the disappearance, F7.3.AUDIT_SEES_EVERY_REGISTRATION catches the blind spot, F7.2.SURFACE_PARTITION_TOTAL catches a dead map entry -- where a false positive costs a report instead of a deploy. The counts in this key are what smoke.py compares against; a route that vanishes changes them and fails there."
  }
}